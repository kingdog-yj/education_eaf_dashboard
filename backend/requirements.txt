fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
pandas
pyarrow
numpy
claude-agent-sdk
httpx        # fastapi.testclient(TestClient) 의존
pytest
pytest-asyncio

# 향후 사내 DB 연결 시 활성화 (SqlHeatRepository)
# pyodbc      # MSSQL
# oracledb    # Oracle
